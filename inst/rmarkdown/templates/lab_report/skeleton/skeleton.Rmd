---
title: "Untitled"
header:
  - pos: lo
    next: \headmark
  - pos: ro
    next: name
footer:
  pos: co
  next: \thepage
output: rmdtemplates::lab_report
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  fig.width = 2.3,
  fig.height = 2.1,
  dev.args = list(pointsize = 7)
)

knitr::knit_hooks$set(crop = knitr::hook_pdfcrop)

options(digits = 2)

library(lattice)
library(latticeExtra)
library(gridExtra)
lattice.options(default.theme = list(fontsize = list(text = 7, points = 5)))
```

# Introduction
