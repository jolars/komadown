---
title: Untitled
author: Me
automark: true
header:
  - pos: ro
    first: Text on first page
    next: Text on subsequent pages
output: rmdtemplates::lab_report
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  fig.width = 2.3,
  fig.height = 2.1,
  dev = "cairo_pdf"
)

knitr::knit_hooks$set(crop = knitr::hook_pdfcrop)

options(digits = 3)

# library(lattice)
# library(latticeExtra)
# lattice.options(default.theme = latticework::latticework_theme(
#   fontsize = list(text = 7, points = 5)
# ))
```

# Introduction
